#include<stdio.h>
#include<conio.h>

main()
{
	int n,first_digit,last_digit;
	clrscr();

	printf("Enter any Number : ");
	scanf("%d",&n);

	last_digit = n%10;

	while(n>10)
	{
		n=n/10;
	}
	first_digit = n;

	printf("The sum of the first and the last digit: %d",first_digit+last_digit);

	getch();
}